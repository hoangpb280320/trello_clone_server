export enum EUserRole {
  admin = 'admin',
  user = 'user',
}

export enum EBoardTheme {
  dark = 'dark',
  light = 'light',
}

export enum EEtaskPriority {
  low = 1,
  medium = 2,
  hight = 3,
  urgent = 4,
}

export enum ETaskStatus {
  normal = 'normal',
  warning = 'warning',
  deadline = 'deadline',
}

export enum EBroadRole {
  leader = 'leader',
  teamMember = 'team_member',
  reviewer = 'reviewer',
}
